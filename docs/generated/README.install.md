# Install

```bash
npx labflow init
```


## Cloudflare
The repository root now contains `wrangler.jsonc` and `src/index.ts`, so it can be imported directly into Cloudflare Workers from the repo root.
